# 项目语境

本仓库维护 Codex Skill `codebase-convergence`，用户可见名称为“代码整理修复大师”。它协调一次有证据、可审计的代码库收敛工作，不替代项目自身的领域规则，也不承诺在证据不足时“自动修好一切”。

## 核心术语

- **发现（Finding）**：一个有文件、行号、运行结果或可复现路径支持的问题或风险。
- **发现准入（Finding admission）**：在候选问题进入证据台账前，对其位置、主张、影响和当前仓库状态进行独立核实。
- **证据台账（Evidence ledger）**：发现的唯一清单；专业 Skill 的结论也必须回收到这里，避免多份互相矛盾的报告。
- **证据基线（Evidence basis）**：一个 Finding 所依据的工作树指纹、相关路径和决定性来源；它标识观察对象，不证明结论正确。
- **新鲜度（Freshness）**：Finding 相对当前证据基线的状态，只能是 `current`、`stale` 或 `unknown`。
- **规范来源（Canonical owner）**：某项事实、规则或契约唯一应被维护的位置。其他位置只引用它。
- **直接修复（Direct repair）**：不改变数值、业务规则、公共接口、交互行为或受保护内容的局部修复。
- **决策包（Decision packet）**：完成发现后一次性提交给用户的所有实质性选择，避免零散追问。
- **架构候选（Architecture candidate）**：可能提升模块 Depth、Leverage 或 Locality，但会改变 Interface、Seam 或所有权的方案。
- **验证矩阵（Verification matrix）**：把每个改动关联到相应的格式化、静态检查、测试、构建、界面或文档验证。
- **受保护内容（Locked content）**：用户明确指定必须原样保留的文字、标题、数据定义或其他内容。
- **证据包（Evidence pack）**：只读工具收集的仓库清单和可验证线索；它不是事实裁决，也不能替代代理追踪。
- **评测案例（Evaluation case）**：隔离的用户请求、仓库夹具和行为不变量；它检查是否越权、是否正确暂停和是否完成验证，而不是匹配固定措辞。
- **托管安装（Managed installation）**：由安装清单记录文件哈希的 Skill 副本；本地修改或无清单的旧副本不允许被自动覆盖。

## 不变量

1. 用户指令、仓库指令、领域词汇与已接受 ADR 优先于推测。
2. 数值、规则、状态转换、公共接口、视觉契约和受保护内容，不在未获授权时改变。
3. 每个事实只有一个规范来源；文档用链接复用，不复制规范。
4. 每一行改动都能追溯到一个发现或用户明确要求。
5. “已验证”只描述实际执行过的检查；未覆盖范围必须作为剩余风险报告。
6. 确定性脚本只采集、物化或校验；规范来源、业务决策和修复授权仍由 Skill 工作流处理。
7. 过期或无法重新定位的证据不能授权修复、合并 Finding 或支持完成声明。
8. Finding 成立不代表建议修复成立；修复方向仍须单独证明必要、相称且保持契约。
